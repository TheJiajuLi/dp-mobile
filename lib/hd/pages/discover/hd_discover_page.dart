import 'package:flutter/material.dart';

class HdDiscoverPage extends StatelessWidget {
  const HdDiscoverPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('发现页'));
  }
}
